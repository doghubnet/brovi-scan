import { calculateConfidence } from "@/lib/local-intelligence";
import { parseNumericGrade } from "@/lib/local-intelligence";
import { resolveAcademicBand } from "./grade-bands";
export function analyzeTranscriptLocally(input:any){const g=parseNumericGrade(input.gpa??input.average??0)??0;const scale=(input.gradeScaleType??"unknown") as any;const band=resolveAcademicBand(g,scale);const score=Math.max(0,Math.min(100, (input.transcriptPresent?25:0)+(input.educationLevel?15:0)+(input.field?15:0)+(scale!=="unknown"?15:5)+(input.targetDegree?15:0)+(input.targetField?10:0)+(input.graduationEvidence?5:0)));const confidence=calculateConfidence({structuredFieldCount:[input.educationLevel,input.field,input.gpa,input.targetDegree].filter(Boolean).length,requiredFieldCount:6,hasExtractedText:false,hasAttachments:Boolean(input.transcriptPresent)});return {score,academicBand:band.label,warning:band.warning,confidence,risks:score<60?["Academic readiness is limited for competitive programs."]:[],recommendations:["Provide clear transcript and grade-scale evidence.","Align target field with prior studies."]};}
