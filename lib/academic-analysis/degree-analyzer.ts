export function analyzeDegreeLocally(input:any){const final=Boolean(input.finalDegreePresent);const provisional=Boolean(input.provisionalDegreePresent);const consistency= final||provisional;return {score: consistency?80:45, risks: consistency?[]:["Degree completion evidence is missing."], recommendations:["Upload final degree if available.","If final degree is pending, upload provisional certificate."]};}
