export function detectFinancialRedFlags(i:any){const flags:string[]=[];if((i.statementPeriodMonths??0)<3) flags.push("Statement period is very short.");if(!i.sourceOfFunds||String(i.sourceOfFunds).trim().length<20) flags.push("Source-of-funds explanation is too short.");if(!i.sponsorRelation) flags.push("Sponsor relation is missing.");if(!i.sponsorProofAvailable) flags.push("Sponsor proof is missing.");if(!i.bankName) flags.push("Bank name is missing.");if(!i.currency) flags.push("Currency is missing.");if(!i.accommodationProof) flags.push("Accommodation proof is missing.");if(String(i.largeDeposits??"").trim().length>0 && String(i.largeDepositsExplanation??"").trim().length<10) flags.push("Large deposits were mentioned without clear explanation.");return flags;}
