"use client";
import { AttachmentStatusBadge } from "./attachment-status-badge";
export type AttachmentRow={id:string;attachment_label:string;file_name:string;file_size_bytes:number|null;status:string;created_at:string;storage_path:string};
export function AttachmentListTable({rows,onView,onReplace,onRemove}:{rows:AttachmentRow[];onView:(r:AttachmentRow)=>void;onReplace:(r:AttachmentRow)=>void;onRemove:(r:AttachmentRow)=>void}){return <div className="card overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr><th>Attachment</th><th>File name</th><th>Size</th><th>Status</th><th>Uploaded at</th><th>Actions</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t hover:bg-slate-50 dark:hover:bg-white/5"><td>{r.attachment_label}</td><td>{r.file_name}</td><td>{r.file_size_bytes?`${(r.file_size_bytes/1024/1024).toFixed(2)} MB`:"—"}</td><td><AttachmentStatusBadge status={r.status} /></td><td>{new Date(r.created_at).toLocaleString()}</td><td className="space-x-2"><button className="btn-secondary px-2 py-1" onClick={()=>onView(r)} type="button">View</button><button className="btn-secondary px-2 py-1" onClick={()=>onReplace(r)} type="button">Replace</button><button className="btn-secondary px-2 py-1" onClick={()=>onRemove(r)} type="button">Remove</button></td></tr>)}</tbody></table></div>;}
