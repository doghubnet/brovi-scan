"use client";
import { useState } from "react"; import { useForm } from "react-hook-form"; import type { AIReport } from "@/types"; import { RecommendationList } from "@/components/reports/RecommendationList"; import { ScoreRing } from "@/components/scores/ScoreRing";
type Field={name:string;label:string;type?:string;textarea?:boolean;placeholder?:string};
export function ScanForm({fields,endpoint,submitLabel="Analyze readiness"}:{fields:Field[];endpoint:string;submitLabel?:string}){const{register,handleSubmit,formState:{errors}}=useForm<Record<string,string>>();const[report,setReport]=useState<AIReport|null>(null);const[loading,setLoading]=useState(false);async function onSubmit(data:Record<string,string>){setLoading(true);const res=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setReport(await res.json());setLoading(false);}return <div className="grid gap-6 lg:grid-cols-[1fr_360px]"><form onSubmit={handleSubmit(onSubmit)} className="card grid gap-4 md:grid-cols-2">{fields.map(f=><label key={f.name} className="label md:col-span-1">{f.label}{f.textarea?<textarea className="input mt-2 min-h-28" placeholder={f.placeholder} {...register(f.name,{required:true})}/>:<input className="input mt-2" type={f.type||"text"} placeholder={f.placeholder} {...register(f.name,{required:true})}/>} {errors[f.name]&&<span className="mt-1 block text-xs text-risk">This field is required.</span>}</label>)}<button className="btn-primary md:col-span-2" type="submit">{loading?"Scanning...":submitLabel}</button></form><aside className="card h-fit">{report?<><ScoreRing score={report.score} label="Readiness"/><h3 className="mt-6 text-lg font-bold">Strengths</h3><RecommendationList items={report.strengths}/><h3 className="mt-6 text-lg font-bold">Recommendations</h3><RecommendationList items={report.recommendations}/><p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800 dark:bg-amber-400/10 dark:text-amber-100">{report.warning}</p></>:<p className="muted">Submit the form to receive percentage-based readiness scoring and step-by-step recommendations.</p>}</aside></div>}
