import { DashboardShell } from "@/components/layout/DashboardShell"; import { ScanForm } from "@/components/forms/ScanForm"; import { UploadBox } from "@/components/forms/UploadBox";
const labels=['Sponsor name','Sponsor relation','Bank name','Statement period','Currency','Opening balance','Closing balance','Average balance','Large deposits','Monthly income estimate','Monthly expenses estimate','Source of funds explanation','Tuition fee amount','Living cost amount','Accommodation proof','Sponsor business or job proof'];
const fields=labels.map(l=>({label:l,name:l.toLowerCase().replaceAll(' ','').replace(/[^a-z]/g,''),textarea:l.includes('explanation')||l.includes('deposits')}));
export default function Page(){return <DashboardShell><h1 className="section-title">Bank Statement Scan</h1><div className="card"><p className="font-semibold">Safe information only: account holder name, bank name, statement period, balances, inflows/outflows, sponsor relation, and source-of-funds explanation. Do not enter card numbers, bank login details, passwords, or private account access.</p></div><UploadBox label="Optional private bank statement support file"/><ScanForm fields={fields} endpoint="/api/analyze-finance" submitLabel="Review financial readiness"/></DashboardShell>}
