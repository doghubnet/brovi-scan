import { DashboardShell } from "@/components/layout/DashboardShell"; import { ScanForm } from "@/components/forms/ScanForm"; import { UploadBox } from "@/components/forms/UploadBox";
const docs=['Passport status','Transcript status','Grade 12 certificate','National entrance exam result','CV status','Motivation letter','Recommendation letter','English proof','Declaration of Value','CIMEA when applicable','Admission status','Pre-enrollment receipt','Family sponsorship letter','Birth certificate when needed','Police clearance when needed','Travel insurance when needed','Translation status','Financial docs status','Other country-specific documents'];
const fields=docs.map(l=>({label:l,name:l.toLowerCase().replaceAll(' ','').replace(/[^a-z]/g,''),placeholder:'Available, Missing, Unclear, Expired, Needs translation, or Needs legalization'}));
export default function Page(){return <DashboardShell><h1 className="section-title">Document Scan</h1><UploadBox label="Optional private document upload"/><ScanForm fields={fields} endpoint="/api/analyze-documents" submitLabel="Review documents"/></DashboardShell>}
