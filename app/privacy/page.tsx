import { TopNav } from "@/components/layout/TopNav"; import { DisclaimerBanner } from "@/components/layout/DisclaimerBanner";
export default function Privacy(){return <><TopNav/><main className="container-page py-12"><div className="card max-w-4xl"><h1 className="section-title">Privacy Policy</h1><div className="mt-6 space-y-4 muted"><p>Brovi Scan is designed to keep student preparation data private. Uploaded files must be stored in private Supabase Storage buckets and never exposed through public URLs.</p><p>We do not ask for card numbers, bank login details, passwords, or private account access. Bank statement review uses safe summary fields only.</p><p>Database access should use Supabase row-level security so users can only access their own rows, while admin access is role-based.</p></div><div className="mt-6"><DisclaimerBanner/></div></div></main></>}
