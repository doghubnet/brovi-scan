import { DashboardShell } from "@/components/layout/DashboardShell"; import { ScanForm } from "@/components/forms/ScanForm"; import { InterviewQuestionCard } from "@/components/forms/InterviewQuestionCard"; import { interviewQuestions } from "@/lib/constants/copy";
const fields=[{label:'Country selection',name:'targetCountry'},{label:'Visa type selection',name:'visaType'},{label:'Difficulty level: Beginner, Normal, or Strict',name:'difficulty'},{label:'Question',name:'question',placeholder:interviewQuestions[0]},{label:'Text answer',name:'answer',textarea:true}];
export default function Page(){return <DashboardShell><h1 className="section-title">Interview Practice Scan</h1><div className="grid gap-4 md:grid-cols-2">{interviewQuestions.slice(0,4).map((q,i)=><InterviewQuestionCard key={q} question={q} index={i}/>)}</div><div className="card"><h2 className="text-xl font-bold">Common weak answers</h2><p className="mt-2 muted">Avoid vague, memorized, inconsistent, exaggerated, or financially unclear answers. Be truthful and specific.</p><p className="mt-2 muted">Timed answer option and one-question-at-a-time mode are represented in this first version UI.</p></div><ScanForm fields={fields} endpoint="/api/analyze-interview-answer" submitLabel="Get answer feedback"/></DashboardShell>}
