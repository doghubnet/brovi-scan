import { DashboardShell } from "@/components/layout/DashboardShell"; import { ScanForm } from "@/components/forms/ScanForm"; import { ProgressStepper } from "@/components/forms/ProgressStepper"; import { ProgramMatchCard } from "@/components/forms/ProgramMatchCard";
const fields=['Full name','Country of origin','Target country','Highest education level','Field of study','GPA or average mark','English level','Budget range','Preferred city type','Preferred intake','Preferred degree level','Work preference','Scholarship need','Available documents','Career goal'].map(l=>({label:l,name:l.toLowerCase().replaceAll(' ','').replace(/[^a-z]/g,'')}));
export default function Page(){return <DashboardShell><h1 className="section-title">Program Match Scan</h1><ProgressStepper steps={["Profile","Preferences","Scoring","Recommendations"]} current={1}/><ProgramMatchCard/><ScanForm fields={fields} endpoint="/api/analyze-program" submitLabel="Get match score"/></DashboardShell>}
